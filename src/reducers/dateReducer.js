// types

const initialState = {
  dates: [],
  dateSelect: {},
  dateFavs: [],
};

export default function dateReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
